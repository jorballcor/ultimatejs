// AND, OR NOT

// AND &&

let older = false;
let suscribed = true;
// console.log(true && true); // true
// console.log(true && false); // false
console.log('AND operator', older && suscribed); // true


// OR ||
console.log('OR operator', older || suscribed); // true

// NOT !
console.log('NOT operator', !older);

let childrensCatalog = !older;
console.log('childrensCatalog', childrensCatalog); // true